---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: getourneau/cf_cli
    tag: alpine-test

inputs:
  - name: buildpack-pipeline
  - name: buildpack-github-release
  - name: env-info
    path: env/

run:
  path: buildpack-pipeline/ci/updatebuildpack/updatebuildpack

params:
  BUILDPACK_NAME:
